{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Remove Experience date-range header block",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Remove the calendar/date-range block from each Experience card header while preserving role/company/location layout.",
      "acceptanceCriteria": [
        "In the Experience section, the Calendar icon is not visible anywhere in experience entries.",
        "In the Experience section, the date text (\"{startDate} - {endDate}\") is not visible anywhere in experience entries.",
        "The Experience card header layout has no empty vertical gap where the removed date block used to be.",
        "Other header elements (role, company, and optional location) still render correctly."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/cv/ExperienceSection.tsx",
          "operation": "modify",
          "description": "Remove the Calendar import and the date-range header JSX block (Calendar icon + startDate–endDate span). Update the header layout so the right-side column only renders when location exists (to avoid an empty gap/column after removing dates), while keeping role and company rendering unchanged."
        }
      ]
    }
  ]
}